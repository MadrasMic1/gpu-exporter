version: "3.9"

services:
  gpu-exporter:
    image: gpu-exporter:latest
    container_name: gpu-exporter
    restart: unless-stopped

    ports:
      - "9835:9835"

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities:
                - gpu